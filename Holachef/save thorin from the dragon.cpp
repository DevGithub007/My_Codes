#include<bits/stdc++.h>
using namespace std;

int main(){
	long long n,p,i,j,k,l,m,n,sum=0,a[555];
	scanf("%lld %lld",&n,&p);
	for(i=0;i<n;i++)
	{
		scanf("%lld",&a[i]);
	}
	l=n;
	while(l>2 && p>sum)
	{
		pow(a[i])
	}
	return 0;
}
